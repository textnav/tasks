import { TagHighlightPipe } from './tag-highlight.pipe'

describe('TagHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new TagHighlightPipe()
    expect(pipe).toBeTruthy()
  })
})
